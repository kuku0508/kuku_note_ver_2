## 今天修改過的筆記
```base
filters:
  and:
    - file.mtime >= (time(now) - dur(24, "hours"))
properties:
  file.name:
    displayName: Name
views:
  - type: table
    name: Default view
    order:
      - file.name
    sort:
      - property: file.name
        direction: ASC

```