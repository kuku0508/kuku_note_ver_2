## 今天修改過的筆記
```base 
properties:
  file.name:
    displayName: file.name
  file.mtime:
    displayName: file.mtime
views:
  - type: table
    name: Default view
    filters: file.mtime >= now() + "-1 day"
    order:
      - file.name
      - file.mtime
      - file.ctime
    sort:
      - property: file.mtime
        direction: ASC
      - property: file.ctime
        direction: ASC
    columnSize:
      file.mtime: 217

```
