## 今天修改過的筆記
```base 
properties:
  file.name:
    displayName: file.name
  file.mtime:
    displayName: file.mtime
views:
  - type: table
    name: Default view
    filters: file.mtime >= now() + "-1 day"
    order:
      - file.name
      - file.name
      - file.mtime
      - file.path

```
