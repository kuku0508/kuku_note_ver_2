```R
# 下載檔案
download <- read.csv(
  "C:/Users/kuku/Downloads/data mining/download.csv",
  header = TRUE,
  fileEncoding = "BIG5"
)

# 創造原始資料資料夾
dir.create("C:/Users/kuku/Downloads/data mining/raw_data")

for (i in seq_len(nrow(download))){
  url <-download$fileURL[i]
  info <- gsub("/","-",download$fileinfo[i])
  name <- paste0(info,".zip")
  
  dest <-file.path("C:/Users/kuku/Downloads/data mining/raw_data",name)
  download.file(url,destfile = dest,mode = "wb")
  }


youbike <- read.csv(
  "C:/Users/kuku/Downloads/202503_youbike.csv",
  header = FALSE,
  col.names = c("借車時間", "借車場站", "還車時間", "還車場站", "借用時長", "車種", "借用日期")
)
```
